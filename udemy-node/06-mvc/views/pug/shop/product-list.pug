extends ../layouts/main-layout.pug

block title
  title #{docTitle}

block content
  main
    h1 All Products 
    section.products
      if products.length > 0
        each product of products
          product-item(data-product=product, data-operations=`["detail", "cart"]`)
      else
        p no product