extends layouts/main-layout.pug

block title
  title Add Product

block content
  main
    form(action="/add-product", method="POST") 
      label(for="name") Name 
      input#name(type="text" name="name")
      input(type="submit") 